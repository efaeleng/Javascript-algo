//Write a JavaScript program to compute the sum and product of an array of integers

const int =[2, 4, 6, 8, 10, 14, 16];
let sum = 0;
let product = 1;

for(let i=0; i < int.length;i++){
   sum+= int[i] 
    product*= int[i]}

console.log(`the sum is ${sum} and the product: ${product}`)